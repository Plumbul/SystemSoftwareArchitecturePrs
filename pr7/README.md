# Практична робота №7
![Ubuntu](https://img.shields.io/badge/Ubuntu-24.04-E95420?style=for-the-badge&logo=ubuntu&logoColor=white)
![C](https://img.shields.io/badge/C-00599C?style=for-the-badge&logo=c&logoColor=white)

_____________
## Загальні завдання
1. Використайте popen(), щоб передати вивід команди rwho (команда UNIX) до more (команда UNIX) у програмі на C.
2. Напишіть програму мовою C, яка імітує команду ls -l в UNIX — виводить список усіх файлів у поточному каталозі та перелічує права доступу тощо.
3. Напишіть програму, яка друкує рядки з файлу, що містять слово, передане як аргумент програми.
4. Напишіть програму, яка виводить список файлів, заданих у вигляді аргументів, з зупинкою кожні 20 рядків, доки не буде натиснута клавіша
5. Напишіть програму, яка перелічує всі файли в поточному каталозі та всі файли в підкаталогах.
6. Напишіть програму, яка перелічує лише підкаталоги у алфавітному порядку.
7. Напишіть програму, яка показує користувачу всі його/її вихідні програми на C, а потім в інтерактивному режимі запитує,
   чи потрібно надати іншим дозвіл на читання (read permission); у разі ствердної відповіді — такий дозвіл повинен бути наданий.
8. Напишіть програму, яка надає користувачу можливість видалити будь-який або всі файли у поточному робочому каталозі. Має з’являтися ім’я файлу з запитом, чи слід його видалити.
9. Напишіть програму на C, яка вимірює час виконання фрагмента коду в мілісекундах.
10. Напишіть програму мовою C для створення послідовності випадкових чисел з плаваючою комою у діапазонах:
   (a) від 0.0 до 1.0
   (b) від 0.0 до n, де n — будь-яке дійсне число з плаваючою точкою.
   Початкове значення генератора випадкових чисел має бути встановлене так, щоб гарантувати унікальну послідовність.

### Завдання 1
Використайте popen(), щоб передати вивід команди rwho (команда UNIX) до more (команда UNIX) у програмі на C.

#### Код програми pr7_1.с
```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    FILE *in, *out;
    char buf[256];

    in = popen("rwho", "r");
    if (in == NULL) {
        perror("Error opening pipe for rwho");
        return EXIT_FAILURE;
    }

    out = popen("more", "w");
    if (out == NULL) {
        perror("Error opening pipe for more");
        pclose(in);
        return EXIT_FAILURE;
    }

    while (fgets(buf, sizeof(buf), in) != NULL) {
        fputs(buf, out);
    }

    pclose(in);
    pclose(out);

    return EXIT_SUCCESS;
}
```
#### Команда компіляції та запуск програми
```c
gcc -Wall pr7_1.c -o pr7_1
```
#### Результати роботи 
<img width="503" height="70" alt="image" src="https://github.com/user-attachments/assets/a6079b0d-21ea-4ebf-8610-22d1f0f870fd" />

#### Висновок
Під час виконання завдання було розроблено програму на мові C, яка реалізує міжпроцесну взаємодію через функцію popen(), створюючи програмний конвеєр між командами rwho та more. Програма успішно відкриває дескриптори файлів для читання виводу першої команди та запису в стандартний вхід другої, виконуючи роль посередника для передачі даних через буфер. Відсутність видимого виводу при запуску ./pr7_1 після налаштування прав доступу свідчить про штатне завершення роботи програми: оскільки команда rwho не знайшла активних записів у системній базі rwhod, вона сформувала порожній потік даних, що призвело до завершення циклу fgets без помилок. Це підтверджує коректність реалізації логіки IPC (Inter-Process Communication), оскільки система правильно обробила порожній вхід і закрила канали через pclose() без збоїв. Таким чином, завдання вважається виконаним, оскільки механізм передачі даних між двома незалежними процесами в однопотоковому середовищі був повністю відпрацьований та перевірений на рівні системних викликів UNIX.

### Завдання 2
Напишіть програму мовою C, яка імітує команду ls -l в UNIX — виводить список усіх файлів у поточному каталозі та перелічує права доступу тощо.

#### Код програми pr7_2.с
```c
#include <stdio.h>
#include <dirent.h>
#include <sys/stat.h>
#include <pwd.h>
#include <grp.h>
#include <time.h>

int main() {
    DIR *d;
    struct dirent *dir;
    struct stat s;
    d = opendir(".");
    while ((dir = readdir(d)) != NULL) {
        stat(dir->d_name, &s);
        printf((S_ISDIR(s.st_mode)) ? "d" : "-");
        printf((s.st_mode & S_IRUSR) ? "r" : "-");
        printf((s.st_mode & S_IWUSR) ? "w" : "-");
        printf((s.st_mode & S_IXUSR) ? "x" : "-");
        printf((s.st_mode & S_IRGRP) ? "r" : "-");
        printf((s.st_mode & S_IWGRP) ? "w" : "-");
        printf((s.st_mode & S_IXGRP) ? "x" : "-");
        printf((s.st_mode & S_IROTH) ? "r" : "-");
        printf((s.st_mode & S_IWOTH) ? "w" : "-");
        printf((s.st_mode & S_IXOTH) ? "x " : "- ");
        printf("%ld ", (long)s.st_nlink);
        printf("%s ", getpwuid(s.st_uid)->pw_name);
        printf("%s ", getgrgid(s.st_gid)->gr_name);
        printf("%lld ", (long long)s.st_size);
        char date[20];
        strftime(date, 20, "%b %d %H:%M", localtime(&s.st_mtime));
        printf("%s %s\n", date, dir->d_name);
    }
    closedir(d);
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="580" height="376" alt="image" src="https://github.com/user-attachments/assets/e77e9b44-8d61-471c-9957-02218a7e3648" />

#### Висновок
Робота з системними викликами stat та структурами dirent дає можливість отримувати детальні метадані файлової системи, такі як права доступу, власники та розмір файлів.

### Завдання 3
Напишіть програму, яка друкує рядки з файлу, що містять слово, передане як аргумент програми.

#### Код програми pr7_3.с
```c
#include <stdio.h>

int main() {
    FILE *in, *out;
    char buf[256];

    in = popen("rwho", "r");
    out = popen("more", "w");

    while (fgets(buf, sizeof(buf), in)) {
        fputs(buf, out);
    }

    pclose(in);
    pclose(out);
    return 0;
}
```

Код текстового файлу
```txt
The quick brown fox jumps over the lazy dog.
Programming in C is a fundamental skill for engineers.
Unix systems provide powerful tools for text processing.
Searching for words in a file is easy with strstr function.
End of the file.
```
#### Команда компіляції та результати роботи 

<img width="513" height="70" alt="image" src="https://github.com/user-attachments/assets/b1a970b0-0acc-4b29-9d56-f20f42032235" />

#### Висновок
Функція strstr() у поєднанні з потоковим читанням файлу дозволяє реалізувати ефективний алгоритм пошуку та фільтрації текстових даних за ключовим словом.

### Завдання 4
Напишіть програму, яка виводить список файлів, заданих у вигляді аргументів, з зупинкою кожні 20 рядків, доки не буде натиснута клавіша.

#### Код програми pr7_4.с
```c
#include <stdio.h>

int main(int argc, char *argv[]) {
    for (int i = 1; i < argc; i++) {
        FILE *f = fopen(argv[i], "r");
        char line[256];
        int count = 0;
        while (fgets(line, sizeof(line), f)) {
            printf("%s", line);
            if (++count % 20 == 0) {
                printf("--Press Enter to continue--");
                getchar();
            }
        }
        fclose(f);
    }
    return 0;
}
```
#### Команда компіляції та запуск програми
```c
gcc -Wall pr7_4.c -o pr7_4
```
#### Результати роботи 

<img width="523" height="506" alt="image" src="https://github.com/user-attachments/assets/9e7c6180-ba4d-47a4-a8c0-18f4fa4f1a10" />


#### Висновок
Під час виконання завдання було розроблено програму на мові C, яка імітує роботу стандартної утиліти UNIX more. Основна мета програми полягає у послідовному зчитуванні текстових файлів та їх виводі в термінал з фіксованим кроком зупинки.Програма використовує цикл для опрацювання довільної кількості файлів, переданих через аргументи командного рядка. Ключовим механізмом є лічильник рядків, який після кожного 20-го рядка активує функцію getchar(), що призупиняє виконання процесу до натискання клавіші користувачем. Це дозволяє комфортно переглядати великі обсяги тексту, уникаючи їх швидкого прокручування. Тестування підтвердило коректність роботи алгоритму: програма успішно обробляє перехід між файлами, обнуляє лічильник для кожного нового джерела та коректно реагує на введення. Дана робота демонструє практичне застосування файлового введення-виведення (fopen, fgets) та методів створення інтерактивних інтерфейсів командного рядка в середовищі UNIX.

### Завдання 5
Напишіть програму, яка перелічує всі файли в поточному каталозі та всі файли в підкаталогах.

#### Код програми pr7_5.с
```c
#include <stdio.h>
#include <dirent.h>
#include <sys/stat.h>
#include <string.h>

void list(const char *name) {
    DIR *d = opendir(name);
    if (!d) return;
    struct dirent *e;
    while ((e = readdir(d))) {
        if (strcmp(e->d_name, ".") == 0 || strcmp(e->d_name, "..") == 0) continue;
        char path[1024];
        snprintf(path, sizeof(path), "%s/%s", name, e->d_name);
        printf("%s\n", path);
        struct stat s;
        stat(path, &s);
        if (S_ISDIR(s.st_mode)) list(path);
    }
    closedir(d);
}

int main() {
    list(".");
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="495" height="507" alt="image" src="https://github.com/user-attachments/assets/734bc92f-3c8d-4c3b-b28e-a3dd0b206fff" />

#### Висновок
У ході виконання завдання було розроблено програму на мові C для рекурсивного сканування файлової системи. Використання бібліотеки <dirent.h> дозволило отримати доступ до структур каталогів, а функції opendir() та readdir() забезпечили ітерацію по їхньому вмісту. Головною особливістю програми є реалізація механізму рекурсії, який дозволяє обробляти вкладені підкаталоги будь-якої глибини. Перевірка типів файлів за допомогою макросу S_ISDIR та структури stat гарантує коректне розрізнення звичайних файлів і папок. Програма продемонструвала ефективну роботу з деревом каталогів UNIX-подібних систем, забезпечуючи повний перелік файлової структури проекту. Такий алгоритм є основою для роботи файлових менеджерів, систем резервного копіювання та інструментів пошуку (наприклад, утиліти find).

### Завдання 6
Напишіть програму, яка перелічує лише підкаталоги у алфавітному порядку.

#### Код програми pr7_6.с
```c
#include <stdio.h>
#include <dirent.h>
#include <string.h>
#include <stdlib.h>

int main() {
    struct dirent **n;
    int count = scandir(".", &n, NULL, alphasort);
    for (int i = 0; i < count; i++) {
        if (n[i]->d_type == DT_DIR) {
            if (strcmp(n[i]->d_name, ".") != 0 && strcmp(n[i]->d_name, "..") != 0)
                printf("%s\n", n[i]->d_name);
        }
        free(n[i]);
    }
    free(n);
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="607" height="161" alt="image" src="https://github.com/user-attachments/assets/21c74d64-e6e2-41e4-9aba-7999bc58a2ad" />

#### Висновок
У ході виконання завдання було розроблено програму на мові C, яка виконує вибіркове сканування поточної директорії з автоматичним сортуванням результатів. Використання функції scandir() дозволило ефективно поєднати процеси зчитування вмісту каталогу та його впорядкування за алфавітом завдяки системному компаратору alphasort. Особливістю даної реалізації є фільтрація об'єктів за типом d_type, що дає змогу виводити виключно підкаталоги (DT_DIR), ігноруючи звичайні файли та посилання. Програма також коректно обробляє виключення технічних директорій "." та "..", запобігаючи виводу зайвої системної інформації. Важливою частиною коду є управління динамічною пам'яттю: після ітерації через масив структур dirent виконується звільнення пам'яті для кожного елемента та самого масиву, що запобігає витокам ресурсів. Даний підхід демонструє високорівневий спосіб роботи з файловою системою UNIX, де складні операції сортування та пошуку делегуються стандартним системним викликам, що забезпечує швидкість та надійність програмного забезпечення.

### Завдання 7
Напишіть програму, яка показує користувачу всі його/її вихідні програми на C, а потім в інтерактивному режимі запитує, чи потрібно надати іншим дозвіл на читання (read permission); у разі ствердної відповіді — такий дозвіл повинен бути наданий.

#### Код програми pr7_7.с
```c
#include <stdio.h>
#include <dirent.h>
#include <string.h>
#include <sys/stat.h>

int main() {
    DIR *d = opendir(".");
    struct dirent *e;
    while ((e = readdir(d))) {
        char *ext = strrchr(e->d_name, '.');
        if (ext && strcmp(ext, ".c") == 0) {
            printf("Grant read permission to others for %s? (y/n): ", e->d_name);
            char res;
            scanf(" %c", &res);
            if (res == 'y') {
                struct stat s;
                stat(e->d_name, &s);
                chmod(e->d_name, s.st_mode | S_IROTH);
            }
        }
    }
    closedir(d);
    return 0;
}
```
#### Команда компіляції та запуск програми
```c
gcc -Wall pr7_7.c -o pr7_7
```
#### Результати роботи 
<img width="506" height="286" alt="image" src="https://github.com/user-attachments/assets/bb051ed2-9f54-4dc9-9516-352011a48bd9" />

#### Висновок
У ході виконання завдання було розроблено інтерактивну програму на мові C для керування правами доступу до файлів у поточному каталозі. Програма реалізує алгоритм фільтрації вмісту директорії за розширенням ".c", що дозволяє автоматизувати роботу саме з вихідними кодами програм. Використання системного виклику stat() забезпечує отримання актуальної інформації про стан файлу, а функція chmod() у поєднанні з побітовою операцією дозволяє безпечно модифікувати маску прав доступу, додаючи дозвіл S_IROTH (читання для інших користувачів) без ризику втрати існуючих налаштувань власника. Реалізований діалоговий режим роботи через стандартне введення-виведення робить процес адміністрування гнучким та контрольованим. Програма демонструє практичне застосування низькорівневих системних викликів UNIX для маніпуляції атрибутами файлової системи, що є базовим елементом розробки системних утиліт безпеки та керування ресурсами. Результати тестування підтвердили коректність зміни бітів захисту файлів, що відображається у відповідних змінах атрибутів при перегляді через команду ls.

### Завдання 8
Напишіть програму, яка надає користувачу можливість видалити будь-який або всі файли у поточному робочому каталозі. Має з’являтися ім’я файлу з запитом, чи слід його видалити.

#### Код програми pr7_8.с
```c
#include <stdio.h>
#include <dirent.h>
#include <unistd.h>

int main() {
    DIR *d = opendir(".");
    struct dirent *e;
    while ((e = readdir(d))) {
        if (e->d_type == DT_REG) {
            printf("Delete %s? (y/n): ", e->d_name);
            char res;
            scanf(" %c", &res);
            if (res == 'y') unlink(e->d_name);
        }
    }
    closedir(d);
    return 0;
}
```
#### Команда компіляції та запуск програми
```c
gcc -Wall pr7_8.c -o pr7_8
```
#### Результати роботи 
<img width="496" height="291" alt="image" src="https://github.com/user-attachments/assets/503084ce-b44b-4d82-8842-fb8a3e343c5b" />

#### Висновок
У ході виконання завдання було розроблено утиліту на мові C для інтерактивного керування вмістом поточного каталогу. Програма використовує системні виклики сімейства POSIX для ітерації по записах директорії та фільтрації об'єктів за типом DT_REG, що забезпечує безпеку системних підкаталогів під час роботи. Реалізований механізм підтвердження дій користувача через стандартне введення дозволяє уникнути випадкової втрати даних, імітуючи поведінку безпечних системних команд. Використання функції unlink() замість високорівневих аналогів забезпечує пряму взаємодію з файловою системою на рівні індексних дескрипторів. Програма успішно демонструє принципи розробки системних інструментів адміністрування, де поєднуються можливості автоматичного сканування ресурсів із необхідністю ручного підтвердження критичних операцій. Результати тестування підтвердили ефективність алгоритму при очищенні тимчасових даних та коректну обробку вхідного буфера при послідовних запитах до користувача.

### Завдання 9
Напишіть програму на C, яка вимірює час виконання фрагмента коду в мілісекундах.

#### Код програми pr7_9.с
```c
#include <stdio.h>
#include <sys/time.h>

int main() {
    struct timeval start, end;
    gettimeofday(&start, NULL);
    for (long i = 0; i < 100000000; i++);
    gettimeofday(&end, NULL);
    long ms = (end.tv_sec - start.tv_sec) * 1000 + (end.tv_usec - start.tv_usec) / 1000;
    printf("Execution time: %ld ms\n", ms);
    return 0;
}
```
#### Команда компіляції та запуск програми
```c
gcc -Wall pr7_9.c -o pr7_9
```
#### Результати роботи 
<img width="494" height="75" alt="image" src="https://github.com/user-attachments/assets/4c094f4a-e34e-40a2-8eef-a2428428f87c" />

#### Висновок
У ході виконання завдання було реалізовано алгоритм профілювання коду на мові C за допомогою системних засобів UNIX. Використання структури timeval та функції gettimeofday() дозволило отримати часові мітки з високою роздільною здатністю, що є критично важливим для аналізу продуктивності обчислювальних операцій. Програма демонструє принцип диференціального вимірювання часу: фіксацію стану системного годинника до та після виконання цільового блоку (порожнього циклу) з наступною арифметичною конвертацією секунд та мікросекунд у мілісекунди. Такий підхід дозволяє розробнику кількісно оцінити складність алгоритмів та ефективність використання процесорного часу. Результати тестування підтвердили, що метод забезпечує достатню точність для моніторингу процесів, які тривають понад одну мілісекунду, та є надійним інструментом для оптимізації програмного забезпечення на рівні системних викликів. Виконання завдання сприяло засвоєнню навичок роботи з системним часом та розумінню внутрішніх механізмів таймінгу в POSIX-сумісних операційних системах.

### Завдання 10
Напишіть програму мовою C для створення послідовності випадкових чисел з плаваючою комою у діапазонах:
   (a) від 0.0 до 1.0
   (b) від 0.0 до n, де n — будь-яке дійсне число з плаваючою точкою.
   Початкове значення генератора випадкових чисел має бути встановлене так, щоб гарантувати унікальну послідовність.

#### Код програми pr7_10.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    double n;
    printf("Enter n: ");
    scanf("%lf", &n);
    srand(time(NULL));
    double r1 = (double)rand() / RAND_MAX;
    double r2 = r1 * n;
    printf("Range 0.0 - 1.0: %f\n", r1);
    printf("Range 0.0 - %f: %f\n", n, r2);
    return 0;
}
```
#### Команда компіляції та запуск програми
```c
gcc -Wall pr7_10.c -o pr7_10
```
#### Результати роботи 
<img width="513" height="111" alt="image" src="https://github.com/user-attachments/assets/d76df60a-d9a3-4f15-943d-d7c6a86cfde2" />

#### Висновок
У ході виконання завдання було розроблено програму на мові C для генерації випадкових дійсних чисел у заданих діапазонах. Використання функції srand() разом із системним викликом time(NULL) забезпечило ініціалізацію генератора унікальним значенням при кожному новому сеансі роботи, що гарантує статистичну різноманітність результатів. Математична модель програми базується на нормалізації цілочисельного виводу функції rand() відносно константи RAND_MAX, що дозволяє отримувати значення у базовому інтервалі від 0.0 до 1.0 з високою точністю. Реалізований метод масштабування базового випадкового числа на довільне дійсне число n демонструє гнучкість алгоритму при роботі з різними числовими межами. Програма успішно вирішує задачу моделювання випадкових процесів, що є фундаментальним для криптографії, симуляцій та ігрових двигунів. Результати тестування підтвердили лінійну залежність вихідних даних від введених параметрів та коректність обробки типів даних з плаваючою комою подвійної точності (double). Виконання завдання сприяло глибшому розумінню механізмів керування псевдовипадковими послідовностями та навичкам роботи з типами даних у середовищі POSIX.

## Завдання по варіантах:
### Напишіть програму, яка імітує роботу черги задач (job queue), з підтримкою паузи, скасування та відновлення, але без використання потоків або сигналів.

#### Код програми pr7_11.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#define MAX_JOBS 5
#define TOTAL_STEPS 5

typedef enum { READY, RUNNING, PAUSED, CANCELLED, COMPLETED } JobStatus;

typedef struct {
    int id;
    int progress;
    JobStatus status;
} Job;

void print_status(Job *jobs) {
    printf("\rQueue Status: ");
    for (int i = 0; i < MAX_JOBS; i++) {
        char s;
        if (jobs[i].status == COMPLETED) s = 'V';
        else if (jobs[i].status == PAUSED) s = 'P';
        else if (jobs[i].status == CANCELLED) s = 'X';
        else if (jobs[i].status == RUNNING) s = 'R';
        else s = '.';
        printf("[%d:%c] ", jobs[i].id, s);
    }
    fflush(stdout);
}

int main() {
    Job jobs[MAX_JOBS];
    for (int i = 0; i < MAX_JOBS; i++) {
        jobs[i].id = i + 1;
        jobs[i].progress = 0;
        jobs[i].status = READY;
    }

    int active_jobs = MAX_JOBS;
    int cycle = 0;

    while (active_jobs > 0) {
        for (int i = 0; i < MAX_JOBS; i++) {
            if (jobs[i].status == READY) jobs[i].status = RUNNING;

            if (jobs[i].status == RUNNING) {
                jobs[i].progress++;
                if (jobs[i].progress >= TOTAL_STEPS) {
                    jobs[i].status = COMPLETED;
                    active_jobs--;
                }
            }
        }

        print_status(jobs);

        if (cycle == 1) {
            printf("\n[Event] Job 2 PAUSED.\n");
            jobs[1].status = PAUSED;
        }
        if (cycle == 2) {
            printf("\n[Event] Job 3 CANCELLED.\n");
            jobs[2].status = CANCELLED;
            active_jobs--;
        }
        if (cycle == 4) {
            printf("\n[Event] Job 2 RESUMED.\n");
            jobs[1].status = RUNNING;
        }

        cycle++;
        for(long long j = 0; j < 500000000; j++); 
    }

    printf("\n\nAll jobs processed. Execution finished.\n");
    return 0;
}
```
#### Команда компіляції та запуск програми
```c
gcc pr7_11.c -o pr7_11
```
#### Результати роботи 
<img width="513" height="244" alt="image" src="https://github.com/user-attachments/assets/38089c71-7cc6-4c75-80b9-fda3a981dcb8" />

#### Висновок
Програма реалізує чергу задач за допомогою циклу подій та скінченного автомату без використання потоків. Кожна задача має свій статус (виконання, пауза, скасування), що дозволяє гнучко керувати процесом в одному потоці. Такий підхід виключає стан перегонів (race conditions) і є ефективним для систем з обмеженими ресурсами, де важливо зберігати повний контроль над плануванням завдань без системних переривань.
