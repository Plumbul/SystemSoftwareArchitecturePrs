# Практична робота №9
![Ubuntu](https://img.shields.io/badge/Ubuntu-24.04-E95420?style=for-the-badge&logo=ubuntu&logoColor=white)
![C](https://img.shields.io/badge/C-00599C?style=for-the-badge&logo=c&logoColor=white)

_____________
## Загальні завдання
1. Напишіть програму, яка читає файл /etc/passwd за допомогою команди getent passwd, щоб дізнатись, які облікові записи визначені на вашому комп’ютері.Програма повинна визначити, чи є серед них звичайні користувачі (ідентифікатори UID повинні бути більші за 500 або 1000, залежно від вашого дистрибутива), окрім вас.
2. Напишіть програму, яка виконує команду cat /etc/shadow від імені адміністратора, хоча запускається від звичайного користувача.
(Ваша програма повинна робити необхідне, виходячи з того, що конфігурація системи дозволяє отримувати адміністративний доступ за допомогою відповідної команди.)
3. Напишіть програму, яка від імені root копіює файл, який вона перед цим створила від імені звичайного користувача. Потім вона повинна помістити копію у домашній каталог звичайного користувача. Далі, використовуючи звичайний обліковий запис, програма намагається змінити файл і зберегти зміни. Що відбудеться? Після цього програма намагається видалити цей файл за допомогою команди rm. Що відбудеться?
4. Напишіть програму, яка по черзі виконує команди whoami та id, щоб перевірити стан облікового запису користувача, від імені якого вона запущена. Є ймовірність, що команда id виведе список різних груп, до яких ви належите. Програма повинна це продемонструвати.
5. Напишіть програму, яка створює тимчасовий файл від імені звичайного користувача. Потім від імені суперкористувача використовує команди chown і chmod, щоб змінити тип володіння та права доступу. Програма повинна визначити, в яких випадках вона може виконувати читання та запис файлу, використовуючи свій обліковий запис.
6. Напишіть програму, яка виконує команду ls -l, щоб переглянути власника і права доступу до файлів у своєму домашньому каталозі, в /usr/bin та в /etc. Продемонструйте, як ваша програма намагається обійти різні власники та права доступу користувачів, а також здійснює спроби читання, запису та виконання цих файлів.

### Завдання 1
Напишіть програму, яка читає файл /etc/passwd за допомогою команди getent passwd, щоб дізнатись, які облікові записи визначені на вашому комп’ютері. Програма повинна визначити, чи є серед них звичайні користувачі (ідентифікатори UID повинні бути більші за 500 або 1000, залежно від вашого дистрибутива), окрім вас.

#### Код програми pr9_1.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
    FILE *fp = popen("getent passwd", "r");
    if (!fp) return 1;

    char line[256], user[50];
    int uid;
    uid_t my_uid = getuid();

    printf("Інші звичайні користувачі:\n");
    while (fgets(line, sizeof(line), fp)) {
        if (sscanf(line, "%[^:]:%*[^:]:%d", user, &uid) == 2) {
            if (uid >= 1000 && uid < 65534 && uid != (int)my_uid) {
                printf("- %s (UID: %d)\n", user, uid);
            }
        }
    }

    pclose(fp);
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="501" height="76" alt="image" src="https://github.com/user-attachments/assets/70732f8e-e57e-4d46-b63b-65c2d8631c0c" />

#### Висновок
У ході виконання завдання програма успішно здійснила запит до системної бази даних паролів через конвеєр popen(). Відсутність записів у фінальному виводі при збереженні структури заголовка свідчить про те, що в даній операційній системі відсутні сторонні облікові записи з ідентифікаторами UID у діапазоні від 1000 до 65533. Це підтверджує коректність роботи фільтрів, які відсікають системні служби (UID < 1000), поточного користувача та службовий запис nobody. Тестування довело, що програмний комплекс правильно взаємодіє з оболонкою Shell та коректно інтерпретує отримані структуровані дані.

### Завдання 2
Напишіть програму, яка виконує команду cat /etc/shadow від імені адміністратора, хоча запускається від звичайного користувача. (Ваша програма повинна робити необхідне, виходячи з того, що конфігурація системи дозволяє отримувати адміністративний доступ за допомогою відповідної команди.)
#### Код програми pr9_2.с
```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("Спроба отримати доступ до /etc/shadow через sudo...\n");

    int status = system("sudo cat /etc/shadow");

    if (status == -1) {
        perror("Помилка виконання system");
        return 1;
    }

    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="476" height="581" alt="image" src="https://github.com/user-attachments/assets/f134f02c-e911-4116-8c92-0188d95bde19" />

#### Висновок
У ході виконання завдання було розроблено програму, яка демонструє взаємодію прикладного коду з механізмами розмежування прав доступу в ОС UNIX. Використання системного виклику system() у поєднанні з утилітою sudo дозволило програмі, запущеній від імені звичайного користувача, отримати доступ до захищеного системного файлу /etc/shadow, що містить хешовані паролі. Експеримент підтвердив, що безпека системи базується на делегуванні адміністративних повноважень через зовнішні конфігураційні файли (sudoers), а не лише на правах самого виконуваного файлу. Даний підхід ілюструє стандартний метод виконання привілейованих операцій у системному програмуванні, коли програма виступає ініціатором запиту на підвищення прав, підпорядковуючись при цьому загальній політиці безпеки операційного середовища.

### Завдання 3
Напишіть програму, яка від імені root копіює файл, який вона перед цим створила від імені звичайного користувача. Потім вона повинна помістити копію у домашній каталог звичайного користувача. Далі, використовуючи звичайний обліковий запис, програма намагається змінити файл і зберегти зміни. Що відбудеться? Після цього програма намагається видалити цей файл за допомогою команди rm. Що відбудеться?
#### Код програми pr9_3.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
    const char *filename = "test_file.txt";
    const char *copy_name = "root_copy.txt";
    FILE *f;

    f = fopen(filename, "w");
    if (f == NULL) { perror("Помилка створення"); return 1; }
    fprintf(f, "Початковий текст від користувача.\n");
    fclose(f);
    printf("1. Файл '%s' створено.\n", filename);

    char cmd[256];
    sprintf(cmd, "sudo cp %s %s", filename, copy_name);
    system(cmd);
    printf("2. Копію '%s' створено від імені root.\n", copy_name);

    printf("3. Спроба змінити файл '%s'...\n", copy_name);
    f = fopen(copy_name, "a");
    if (f == NULL) {
        printf("   РЕЗУЛЬТАТ: Помилка доступу (Permission denied). Неможливо змінити.\n");
    } else {
        fprintf(f, "Спроба запису.\n");
        fclose(f);
    }

    printf("4. Спроба видалити файл '%s' за допомогою rm...\n", copy_name);
    int res = system("rm root_copy.txt");
    if (res == 0) {
        printf("   РЕЗУЛЬТАТ: Файл видалено успішно.\n");
    } else {
        printf("   РЕЗУЛЬТАТ: Не вдалося видалити файл.\n");
    }

    unlink(filename);
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="628" height="204" alt="image" src="https://github.com/user-attachments/assets/d61e5c2d-976d-4817-8b0f-e499309f2a09" />

#### Висновок
У ході експерименту було досліджено механізми володіння файлами та ієрархію прав доступу в системі Linux. Встановлено, що при копіюванні об'єкта через sudo права власності переходять до суперкористувача, що унеможливлює подальше редагування вмісту звичайним користувачем через обмеження прав на запис (write bit). Проте, спроба видалення файлу виявилася успішною, оскільки операція видалення (unlink) технічно є модифікацією батьківського каталогу, а не самого файлу. Таким чином було підтверджено важливий принцип системної безпеки: володіння каталогом надає право на керування його структурою, включаючи видалення файлів будь-якого іншого користувача, якщо не встановлено спеціальний захисний біт (sticky bit). Дане завдання наочно демонструє різницю між правами на контент файлу та правами на його існування в межах файлової системи.

### Завдання 4
Напишіть програму, яка по черзі виконує команди whoami та id, щоб перевірити стан облікового запису користувача, від імені якого вона запущена. Є ймовірність, що команда id виведе список різних груп, до яких ви належите. Програма повинна це продемонструвати.
#### Код програми pr9_4.с
```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("--- Перевірка поточного облікового запису ---\n\n");

    printf("Результат команди 'whoami': ");
    fflush(stdout);
    system("whoami");

    printf("\n--- Детальна інформація про ідентифікатори ---\n");

    system("id");

    printf("\n--------------------------------------------\n");
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="820" height="224" alt="image" src="https://github.com/user-attachments/assets/3588759f-11a9-417e-8a52-75e3d6c78391" />

#### Висновок
У ході виконання завдання було розроблено програмний інструмент для діагностики ідентифікаційних параметрів поточного процесу. Використання системних утиліт whoami та id дозволило наочно продемонструвати структуру облікового запису в багатокористувацькій системі. Експеримент показав, що ідентифікація користувача не обмежується лише іменем, а включає складну ієрархію числових ідентифікаторів (UID/GID) та членство у декількох групах одночасно, що визначає сукупність прав доступу до різних ресурсів системи. Дана програма підтверджує, що системне оточення автоматично наслідує привілеї користувача, який ініціював запуск, а вивід команди id є ключовим для розуміння матриці прав доступу, особливо в контексті групової політики безпеки UNIX-подібних операційних систем.

### Завдання 5
Напишіть програму, яка створює тимчасовий файл від імені звичайного користувача. Потім від імені суперкористувача використовує команди chown і chmod, щоб змінити тип володіння та права доступу. Програма повинна визначити, в яких випадках вона може виконувати читання та запис файлу, використовуючи свій обліковий запис.
#### Код програми pr9_5.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

void check_access(const char *filename) {
    printf("  Доступ на ЧИТАННЯ: %s\n", access(filename, R_OK) == 0 ? "ТАК" : "НІ");
    printf("  Доступ на ЗАПИС:   %s\n", access(filename, W_OK) == 0 ? "ТАК" : "НІ");
}

void run_cmd(const char *cmd) {
    printf("\nВиконується: %s\n", cmd);
    system(cmd);
}

int main() {
    const char *file = "test_perms.txt";

    FILE *f = fopen(file, "w");
    if (!f) { perror("Помилка"); return 1; }
    fprintf(f, "Конфіденційні дані\n");
    fclose(f);
    printf("1. Файл створено. Власник — ви.\n");
    check_access(file);

    char cmd[256];
    sprintf(cmd, "sudo chown root:root %s", file);
    run_cmd(cmd);

    run_cmd("sudo chmod 644 test_perms.txt");
    printf("Результат для 644 (Ви — 'others'):\n");
    check_access(file);

    run_cmd("sudo chmod 600 test_perms.txt");
    printf("Результат для 600 (Ви — 'others'):\n");
    check_access(file);

    run_cmd("sudo chmod 666 test_perms.txt");
    printf("Результат для 666 (Ви — 'others'):\n");
    check_access(file);

    system("sudo rm test_perms.txt");
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="502" height="487" alt="image" src="https://github.com/user-attachments/assets/ca07095b-21f8-4764-ab7a-4491bc9f0a64" />

#### Висновок
У ході експерименту було продемонстровано динамічну зміну повноважень процесу щодо файлового об'єкта при зміні його атрибутів власника та маски доступу. Використання системних викликів chown та chmod через механізм привілейованого виконання sudo дозволило штучно створити умови, за яких звичайний користувач втрачає або частково повертає права на власноруч створений файл. Перевірка за допомогою функції access() підтвердила, що операційна система суворо дотримується трирівневої моделі прав (User-Group-Others). Зокрема, встановлено, що після зміни власника на root, доступ користувача до файлу повністю визначається бітами прав для категорії "інші", незалежно від того, хто був первинним творцем файлу. Дана робота наочно ілюструє базові принципи адміністрування безпеки в UNIX-системах, де права доступу є атрибутом файлового дескриптора в іноді (inode) і перевіряються ядром при кожному зверненні до об'єкта.

### Завдання 6
Напишіть програму, яка виконує команду ls -l, щоб переглянути власника і права доступу до файлів у своєму домашньому каталозі, в /usr/bin та в /etc. Продемонструйте, як ваша програма намагається обійти різні власники та права доступу користувачів, а також здійснює спроби читання, запису та виконання цих файлів.

#### Код програми pr9_6.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>
#include <string.h>

void explore_dir(const char *path) {
    char cmd[256];
    printf("\n=== Огляд каталогу: %s ===\n", path);
    sprintf(cmd, "ls -l %s | head -n 5", path);
    system(cmd);
}

void try_write(const char *filepath) {
    printf("Спроба запису у %s: ", filepath);
    int fd = open(filepath, O_WRONLY);
    if (fd == -1) {
        printf("ВІДМОВЛЕНО (%s)\n", strerror(errno));
    } else {
        printf("УСПІШНО\n");
        close(fd);
    }
}

int main() {
    explore_dir("~");
    explore_dir("/usr/bin");
    explore_dir("/etc"); 

    printf("\n--- Експерименти з обходом прав ---\n");

    try_write("/usr/bin/test_file");

    printf("Спроба читання /etc/shadow: ");
    FILE *f = fopen("/etc/shadow", "r");
    if (f == NULL) {
        printf("ВІДМОВЛЕНО (Permission denied)\n");
    } else {
        printf("УСПІШНО\n");
        fclose(f);
    }

    printf("Спроба виконання /usr/bin/whoami: ");
    if (access("/usr/bin/whoami", X_OK) == 0) {
        printf("УСПІШНО (є права на виконання)\n");
    } else {
        printf("ВІДМОВЛЕНО\n");
    }

    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="680" height="617" alt="image" src="https://github.com/user-attachments/assets/74af51db-0d58-49a7-93f3-6d5aba4c02be" />

#### Висновок
У ході виконання даного завдання було проведено комплексне дослідження системи розмежування прав доступу в ОС Linux на прикладі трьох типів каталогів: користувацького, системного бінарного та конфігураційного. Використання команди ls -l у поєднанні з програмними спробами відкриття файлів дозволило наочно побачити роботу механізмів захисту ядра. Експериментально підтверджено, що система ефективно блокує спроби модифікації системних областей (/usr/bin) та читання критичних даних (/etc/shadow) звичайним користувачем, попри наявність прав на виконання стандартних утиліт. Програма продемонструвала, що права доступу є багаторівневими: користувач може одночасно мати дозвіл на запуск програми, але бути повністю обмеженим у праві її зміни або перегляду її секретних даних. Це підкреслює надійність моделі безпеки UNIX, де привілеї чітко розмежовані між власником, групою та іншими користувачами системи.

### Завдання по варіантах
## Створіть файл із нестандартними правами доступу та перевірте його поведінку в різних файлових системах (ext4, tmpfs, NFS).

#### Код програми pr9_7.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <sys/stat.h>
#include <unistd.h>
#include <fcntl.h>
#include <errno.h>
#include <string.h>

void test_fs(const char *path) {
    char filename[256];
    sprintf(filename, "%s/test_file_000", path);

    printf("\n--- Тестування в каталозі: %s ---\n", path);

    if (creat(filename, 0000) == -1) {
        printf("Помилка створення: %s (Можливо, ФС не підтримує такі права)\n", strerror(errno));
        return;
    }

    int fd = open(filename, O_RDONLY);
    if (fd == -1) {
        printf("Результат для прав 000: Доступ заборонено (Очікувана поведінка)\n");
    } else {
        printf("Результат для прав 000: ДОСТУП ДОЗВОЛЕНО (Дивна поведінка ФС!)\n");
        close(fd);
    }

    unlink(filename);
}

int main() {
    test_fs("/tmp");
    test_fs(".");
    test_fs("/mnt/nfs"); 
    
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="759" height="472" alt="image" src="https://github.com/user-attachments/assets/b189ea41-3a2c-4964-a3f6-12ee5025fa75" />

#### Висновок
Дослідження підтвердило, що в Linux права доступу працюють за двома сценаріями: для звичайного користувача маска 000 є абсолютною перешкодою (навіть якщо ти власник), а для root ці обмеження ігноруються завдяки системним привілеям. Експеримент довів, що ядро однаково обробляє права на дисках (ext4) та в пам'яті (tmpfs) через уніфікований рівень VFS. Помилка з NFS лише підкреслила залежність програми від наявності змонтованих мережевих ресурсів. Загалом, робота наочно показала ієрархію безпеки: права доступу захищають дані від користувачів, але адміністратор (root) завжди має повний контроль над системою.
