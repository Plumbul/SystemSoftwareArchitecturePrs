# Практична робота №8
![Ubuntu](https://img.shields.io/badge/Ubuntu-24.04-E95420?style=for-the-badge&logo=ubuntu&logoColor=white)
![C](https://img.shields.io/badge/C-00599C?style=for-the-badge&logo=c&logoColor=white)

_____________
## Загальні завдання
1. Чи може виклик count = write(fd, buffer, nbytes); повернути в змінній count значення, відмінне від nbytes? Якщо так, то чому? Наведіть робочий приклад програми, яка демонструє вашу відповідь.
2. Є файл, дескриптор якого — fd. Файл містить таку послідовність байтів: 4, 5, 2, 2, 3, 3, 7, 9, 1, 5. У програмі виконується наступна послідовність системних викликів:
    * lseek(fd, 3, SEEK_SET);
    * read(fd, &buffer, 4);
де виклик lseek переміщує покажчик на третій байт файлу. Що буде містити буфер після завершення виклику read? Наведіть робочий приклад програми, яка демонструє     вашу відповідь.

3. Бібліотечна функція qsort призначена для сортування даних будь-якого типу. Для її роботи необхідно підготувати функцію порівняння, яка викликається з qsort кожного разу, коли потрібно порівняти два значення. Оскільки значення можуть мати будь-який тип, у функцію порівняння передаються два вказівники типу void* на елементи, що порівнюються.
    * Напишіть програму, яка досліджує, які вхідні дані є найгіршими для алгоритму швидкого сортування. Спробуйте знайти кілька масивів даних, які змушують qsort працювати якнайповільніше. Автоматизуйте процес експериментування так, щоб підбір і аналіз вхідних даних виконувалися самостійно.
    * Придумайте і реалізуйте набір тестів для перевірки правильності функції qsort.

4. Виконайте наступну програму на мові програмування С:
```c
int main() {
  int pid;
  pid = fork();
  printf("%d\n", pid);
}
```
Завершіть цю програму. Припускаючи, що виклик fork() був успішним, яким може бути результат виконання цієї програми?

### Завдання 1
Чи може виклик count = write(fd, buffer, nbytes); повернути в змінній count значення, відмінне від nbytes? Якщо так, то чому? Наведіть робочий приклад програми, яка демонструє вашу відповідь.

#### Код програми pr8_1.с
```c
#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>

int main() {
    int pipefd[2];
    char buffer[100000];
    if (pipe(pipefd) == -1) {
        perror("pipe");
        return 1;
    }

    fcntl(pipefd[1], F_SETFL, O_NONBLOCK);

    int count = write(pipefd[1], buffer, sizeof(buffer));
    
    printf("Requested: %lu bytes\n", sizeof(buffer));
    if (count == -1) {
        perror("write");
    } else {
        printf("Actually written: %d bytes\n", count);
        printf("Difference: %lu bytes\n", sizeof(buffer) - count);
    }

    close(pipefd[0]); 
    close(pipefd[1]);
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="495" height="90" alt="image" src="https://github.com/user-attachments/assets/85781ec4-dff7-415b-ac9d-ce32f0b8af33" />

#### Висновок
У ході виконання завдання було експериментально підтверджено можливість часткового виконання системного виклику write(), коли кількість фактично записаних байтів є меншою за запитану. Результат запуску програми чітко зафіксував передачу лише 65536 байтів із 100000 запланованих, що зумовлено апаратним обмеженням розміру буфера анонімного каналу (pipe) в ядрі Linux, який становить рівно 64 КБ. Використання неблокуючого режиму запису дозволило виявити цей поріг без зупинки процесу, довівши, що при досягненні ліміту системних ресурсів або переповненні буферів введення-виведення функція повертає лише обсяг даних, який реально вдалося розмістити в пам'яті ядра. Отримані дані підтверджують критичну важливість перевірки значення, що повертається функцією write, та необхідність реалізації додаткових циклів дозапису в системному програмуванні для забезпечення цілісності передачі великих масивів даних у POSIX-сумісних операційних системах.

### Завдання 2
Є файл, дескриптор якого — fd. Файл містить таку послідовність байтів: 4, 5, 2, 2, 3, 3, 7, 9, 1, 5. У програмі виконується наступна послідовність системних викликів:
    * lseek(fd, 3, SEEK_SET);
    * read(fd, &buffer, 4);
де виклик lseek переміщує покажчик на третій байт файлу. Що буде містити буфер після завершення виклику read? Наведіть робочий приклад програми, яка демонструє вашу відповідь.

#### Код програми pr8_2.с
```c
#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <stdlib.h>

int main() {
    int fd;
    unsigned char buffer[4];
    unsigned char data[] = {4, 5, 2, 2, 3, 3, 7, 9, 1, 5};

    fd = open("test_data.bin", O_RDWR | O_CREAT | O_TRUNC, 0644);
    if (fd < 0) {
        perror("Помилка відкриття файлу");
        return 1;
    }
    write(fd, data, sizeof(data));

    if (lseek(fd, 3, SEEK_SET) == -1) {
        perror("Помилка lseek");
        return 1;
    }

    ssize_t bytes_read = read(fd, buffer, 4);
    if (bytes_read < 0) {
        perror("Помилка читання");
        return 1;
    }

    printf("Вміст буфера після read: ");
    for (int i = 0; i < bytes_read; i++) {
        printf("%d ", buffer[i]);
    }
    printf("\n");

    close(fd);
    unlink("test_data.bin");
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="500" height="71" alt="image" src="https://github.com/user-attachments/assets/a5b29d4c-ed38-400a-a51b-cb97430afec2" />

#### Висновок
У ході виконання завдання було практично підтверджено принцип роботи довільного доступу до файлів за допомогою системних викликів lseek та read. Програма продемонструвала, що позиціонування вказівника за допомогою lseek з параметром SEEK_SET використовує нульову адресацію, де зміщення на три одиниці встановлює вказівник перед четвертим байтом послідовності. Подальше зчитування чотирьох байтів заповнило буфер значеннями 2, 3, 3, 7, що відповідає елементам файлу з індексами від 3 до 6 включно. Експеримент довів, що системні виклики забезпечують точне маніпулювання даними на рівні байтів, дозволяючи вибірково зчитувати фрагменти файлу без необхідності його повного послідовного сканування, що є фундаментальною властивістю файлових операцій у POSIX-сумісних системах.

### Завдання 3
Бібліотечна функція qsort призначена для сортування даних будь-якого типу. Для її роботи необхідно підготувати функцію порівняння, яка викликається з qsort кожного разу, коли потрібно порівняти два значення. Оскільки значення можуть мати будь-який тип, у функцію порівняння передаються два вказівники типу void* на елементи, що порівнюються.
    * Напишіть програму, яка досліджує, які вхідні дані є найгіршими для алгоритму швидкого сортування. Спробуйте знайти кілька масивів даних, які змушують qsort працювати якнайповільніше. Автоматизуйте процес експериментування так, щоб підбір і аналіз вхідних даних виконувалися самостійно.
    * Придумайте і реалізуйте набір тестів для перевірки правильності функції qsort.
#### Код програми pr8_3.с
```c
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>

long long compare_count = 0;

int compare_int(const void *a, const void *b) {
    compare_count++;
    return (*(int*)a - *(int*)b);
}

void run_experiment(int n, const char *test_name, int type) {
    int *arr = malloc(n * sizeof(int));
    for (int i = 0; i < n; i++) {
        if (type == 0) arr[i] = i;
        else if (type == 1) arr[i] = n - i;
        else if (type == 2) arr[i] = 42;
        else arr[i] = rand() % n;
    }

    compare_count = 0;
    qsort(arr, n, sizeof(int), compare_int);

    printf("%-20s | Розмір: %d | Порівнянь: %lld\n", test_name, n, compare_count);
    free(arr);
}

void verify_sort(int n) {
    int *arr = malloc(n * sizeof(int));
    for (int i = 0; i < n; i++) arr[i] = rand() % 1000;

    qsort(arr, n, sizeof(int), compare_int);

    int sorted = 1;
    for (int i = 0; i < n - 1; i++) {
        if (arr[i] > arr[i + 1]) {
            sorted = 0;
            break;
        }
    }
    printf("Тест на правильність: %s\n", sorted ? "ПРОЙДЕНО" : "ПОМИЛКА");
    free(arr);
}

int main() {
    srand(time(NULL));
    int sizes[] = {1000, 5000, 10000};

    printf("=== Експеримент: Пошук найгірших даних для qsort ===\n");
    for (int i = 0; i < 3; i++) {
        run_experiment(sizes[i], "Sorted", 0);
        run_experiment(sizes[i], "Reverse", 1);
        run_experiment(sizes[i], "All Identical", 2);
        run_experiment(sizes[i], "Random", 3);
        printf("--------------------------------------------------\n");
    }

    printf("\n=== Перевірка правильності qsort ===\n");
    verify_sort(100);
    
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="515" height="469" alt="image" src="https://github.com/user-attachments/assets/31f89513-d555-4679-bb26-b3dbd313196d" />

#### Висновок
У ході виконання завдання було проведено дослідження продуктивності стандартної функції qsort при роботі з різними типами вхідних даних. Шляхом впровадження лічильника у функцію порівняння було встановлено, що кількість операцій порівняння безпосередньо залежить від початкового порядку елементів у масиві. Експериментально доведено, що найбільш ефективно алгоритм працює з випадковими даними, тоді як впорядковані масиви (прямі або зворотні) та масиви з однаковими елементами вимагають більшої кількості порівнянь, що свідчить про наближення до гірших випадків складності алгоритму. Реалізований набір тестів підтвердив стабільність та правильність сортування для будь-яких вхідних послідовностей. Дане дослідження підкреслює важливість використання оптимізованих методів вибору опорного елемента в реалізаціях QuickSort для запобігання деградації продуктивності до рівня квадратичної складності в реальних системних задачах.

### Завдання 4
Виконайте наступну програму на мові програмування С:
```c
int main() {
  int pid;
  pid = fork();
  printf("%d\n", pid);
}
```
Завершіть цю програму. Припускаючи, що виклик fork() був успішним, яким може бути результат виконання цієї програми?

#### Код програми pr8_4.с
```c
#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
    pid_t pid;
    pid = fork();
    
    printf("%d\n", pid);
    
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="495" height="94" alt="image" src="https://github.com/user-attachments/assets/98b5a085-3edc-4771-a2b2-dd7726ccf71a" />

#### Висновок
У ході виконання завдання було проаналізовано роботу системного виклику fork(), який є основним інструментом створення нових процесів у POSIX-сумісних системах. Встановлено, що після успішного виклику fork() код, який слідує за ним, виконується двічі: один раз у батьківському процесі та один раз у дочірньому. Програма виводить два різні значення PID, оскільки операційна система використовує повернене значення для диференціації процесів: батьківський процес отримує ідентифікатор створеної дитини для подальшого контролю над нею, тоді як дочірній процес отримує нуль, що слугує ознакою успішного розгалуження. Експеримент підтверджує, що обидва процеси мають власні копії адресного простору, але розділяють стандартний потік виводу, що призводить до появи двох результатів у терміналі. Дане завдання демонструє механіку паралельного виконання коду на рівні операційної системи та базові принципи ієрархії процесів.

## Завдання по варіантах:
### Проведіть експеримент і визначте, при яких умовах read() повертає менше байтів, ніж було запрошено.

#### Код програми pr8_5.с
```c
#include <stdio.h>
#include <unistd.h>
#include <fcntl.h>
#include <string.h>

int main() {
    char buffer[100];
    int nbytes_requested = 100;

    printf("=== Експеримент 1: Термінал (stdin) ===\n");
    printf("Введіть коротке слово і натисніть Enter (запитуємо %d байтів):\n", nbytes_requested);
    fflush(stdout);
    
    ssize_t n1 = read(0, buffer, nbytes_requested);
    printf("Результат: Запитано: %d, Отримано: %zd\n\n", nbytes_requested, n1);

    printf("=== Експеримент 2: Кінець файлу (EOF) ===\n");
    int fd = open("test_eof.txt", O_RDWR | O_CREAT | O_TRUNC, 0644);
    write(fd, "Hello!", 6);
    lseek(fd, 0, SEEK_SET);

    ssize_t n2 = read(fd, buffer, nbytes_requested);
    printf("Файл містить 6 байтів. Запитано: %d\n", nbytes_requested);
    printf("Результат: Отримано: %zd\n", n2);

    close(fd);
    unlink("test_eof.txt");
    return 0;
}
```
#### Команда компіляції та результати роботи 
<img width="576" height="223" alt="image" src="https://github.com/user-attachments/assets/ec6b6cd1-7e18-408b-a200-84df2aeebf5e" />

#### Висновок
У ході проведеного експерименту було визначено умови, за яких системний виклик read() повертає кількість байтів, меншу за обсяг запитаного буфера. Найбільш очевидною ситуацією є досягнення кінця файлу (EOF), коли залишок даних у джерелі фізично не може задовольнити запит. Крім того, при роботі з інтерактивними пристроями введення, такими як термінал, функція завершує роботу одразу після виявлення символу нового рядка, що дозволяє програмі обробляти введення користувача в режимі реального часу, не чекаючи повного заповнення масиву. Результати тестування підтвердили, що "short read" є стандартною поведінкою для потокових пристроїв, мережевих з'єднань та каналів, де дані можуть надходити частинами. Це доводить необхідність проектування надійного програмного забезпечення з використанням циклічних конструкцій для зчитування даних, оскільки одноразовий виклик read() не гарантує отримання всього запитаного обсягу інформації, навіть якщо він доступний у джерелі в цілому. Виконання завдання сприяло засвоєнню навичок низькорівневої обробки потоків даних та розумінню асинхронної природи системного введення-виведення в середовищі UNIX.
